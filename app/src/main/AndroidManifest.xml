<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.w_rds_pp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_w_rds_app"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_w_rds_app"
        android:supportsRtl="true"
        android:theme="@style/Theme.W_rds_pp">
        <activity
            android:name=".NewGameCreatorActivity"
            android:exported="true"
            android:label="@string/title_activity_new_game_creator"
            android:theme="@style/Theme.W_rds_pp.NoActionBar" />
        <activity
            android:name=".GameActivity"
            android:exported="true"
            android:label="@string/title_activity_game"
            android:theme="@style/Theme.W_rds_pp.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>